[phases.setup]
nixPkgs = [
    "python311",
    "dejavu_fonts",
    "fontconfig",
    "zlib",
    "stdenv.cc.cc.lib"
]

[phases.install]
commands = [
    # Remove the '-y' flag which requires a sysroot argument
    "fc-cache -f -v"  
]

[phases.start]
cmd = "./start.sh"
